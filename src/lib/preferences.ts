/** v2.4 Preferences categories that have their own page (/profile/preferences/<section>). Reminders keeps /profile/reminders. */
export type PrefSection = "appearance" | "tracking" | "privacy" | "account";
export const PREF_SECTIONS: PrefSection[] = ["appearance", "tracking", "privacy", "account"];
